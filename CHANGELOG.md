# CodeLeaf 変更履歴

バージョンごとの主な変更点。`chore`(バージョン更新)、`docs`、`test` などの内部コミットは原則割愛し、利用者向けの変更を中心に記載する。

## 0.9.2 (2026-08-06)
### 修正
- Markdown: 全角の縦棒｜で書かれた表を、表示時に半角へ補正して正しくテーブル表示するように(日本語入力で縦棒が全角確定されると、桁は揃って見えるのに表にならず段落へ流れていた)。補正した表の上には、他の Markdown ビューアでは崩れたまま表示される旨の注意書きを添える

## 0.9.1 (2026-07-14)
### 修正
- コミットグラフ: マージコミットの第2親が既に別レーンに存在する場合に、その親への合流線が描かれない問題を修正

## 0.9.0 (2026-07-07)
### 新機能
- 多言語対応: 英語・日本語に加え、韓国語・簡体字中国語・繁体字中国語・スペイン語・ベトナム語に対応(端末の言語に追従。設定から個別指定も可能)
- Viewer に HTML の整形プレビューを追加(Markdown と同様に Raw 表示へ切替可能)
- ファイル一覧を「最終更新日」で並び替え可能に(最終コミット日時・全体設定に保存)
- メモのコメントを後から編集できるように
- 設定からリリースノート(変更履歴)を閲覧できるように
- 言語選択・メモ保存先・リポジトリ選択のドロップダウンを共通化し角丸に
- リポジトリのアバターを全角2文字に対応。短縮ラベルを3文字にし、彩度を下げ選択リングを濃色に

### 修正
- 3ペインの畳んだアイコンレールで、選択中グループのみ表示しアバター色を抑制
- diff の取得失敗時のメッセージを適切な文言に
- ドロップダウンの文字サイズを周囲のボタンと統一(14sp)

## 0.8.0 (2026-06-26)
### 新機能
- メモ追加シートに「コピー」「共有」導線を追加(保存も実施)
- メモのコピー/共有をファイル毎にグルーピングし、行番号順・引用は3行で省略
- メモ帳を残してエントリだけ全消しするメニューを追加
- メモ追加のコメント欄を、選択した行の内容(最大3行)で初期化
- 整形 Markdown の長押しメモで、ブロック全体でなく長押しした行を捕捉
- メモ追加シートを縦方向に圧縮し、初期表示で操作行まで見せる
- メモ追加シートの保存先選択を枠+▾の明示的な UI に、確定ボタンにアイコンを付与
### 修正
- コミット diff を遅延整形+LazyColumn 化し、大量ファイルでのフリーズ/クラッシュを解消
- メモ追加シートに imePadding を付与し、IME(ソフトキーボード)被りを解消

## 0.7.0 (2026-06-23)
### 新機能
- ファイル一覧に各ファイル/フォルダの最終コミット(著者・更新からの相対時刻)を名前の下へ二行表示。設定「コミット情報を表示」で切替(既定 OFF)
- フォルダの長押しから、そのフォルダ配下を変更したコミット履歴を閲覧
- submodule の長押しから、gitlink(ポインタ)移動の履歴を閲覧
- submodule の差分を、生のハッシュではなく old→new の範囲コミット列(コミットメッセージ・著者・時刻)で表示
- 同期失敗時にスナックバーから「再Clone」(ローカル clone の作り直し)を起動できる導線。リポ編集画面の各行メニュー(⋮)にも「再Clone」を追加
- 同期エラーを「通信エラー」など分かりやすい文言で表示
### 修正
- システムと異なるテーマを選んだときにステータスバーのアイコンが背景へ同化して見えなくなる問題を修正(前面画面のテーマに追従)

## 0.6.0 (2026-06-19)
### 新機能
- ブラウザ上部のタイトル(リポ名)タップでリポ一覧ドロワーを開く
- ビューア上部のファイル名/パス表示タップでブラウザへ戻る(1ペインは閉じる／集中モードは解除)
- ブラウザの検索で、開いていたフォルダをパス絞り込みの既定値に
### 内部・整理(挙動不変)
- 旧名 GitReader を CodeLeaf に統一
- provider 中立な共通モデル／HTTP シームへ集約、OAuth provider を enum 化
- 純粋ロジックの別ファイル分離・命名整理・可視性の最小化・デッドコード除去

## 0.5.0 (2026-06-14)
### 新機能
- ビューアにテキスト選択モードを追加(⋮ メニューでトグル／設定で既定値)
- コミットグラフのブランチ切替中にスピナーを表示
### 修正
- ビューア／ブラウザから戻ったときスクロール位置を保持
- diff の「開く」アイコンをバー右寄せに配置。submodule は未選択で開く

## 0.4.0 (2026-06-11)
### 新機能
- リポを開き直したとき前回のフォルダ/ファイルを復元(設定で切替)
- diff の各ファイルから Viewer で開くアイコン(作業ツリーに無ければその時点の版を表示)
- 集中モード中はリンクで潜った先の戻るで前ファイルへ(集中を維持)

## 0.3.1 (2026-06-09)
### 新機能
- コミットグラフのコミット長押しでリモートブランチへ切替
- メモ追加の行範囲指定をスライダー＋オートリピート(長押し連続増減)に刷新
### 修正
- Markdown のディレクトリ／エンコード済み相対リンクを遷移可能に
- 復元した backStack が削除済みリポを指す「幽霊画面」を一覧へ戻す

## 0.3.0 (2026-06-08)
### 新機能
- submodule 対応を強化: フォルダに「SUB」バッジ、取得をリトライ＋未取得を可視化、SSH URL を HTTPS に書換えて取得
- ファイル長押しメニューに「履歴」を追加
- 多ペインの戻るを操作順に追従(フォルダ遷移直後は一つ上へ)
- diff 下部バーに全ファイル一括折りたたみを追加
- リポ編集カードを clone 進捗表示に対応＋色表現を控えめに
### 修正
- 多ペインのブラウザ右端の余白とファイル名の潰れを是正
- ブラウザ横線のリポ色を是正(色なしはグレー／色変更を即反映)
- diff の縦1ペインの高さを是正
