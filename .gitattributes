# 既定はテキスト自動判定・チェックアウトはLF
* text=auto eol=lf

# Windows 用スクリプトは CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# Gradle ラッパー（Unixで実行するためLF固定）
gradlew text eol=lf

# バイナリ
*.jar binary
*.png binary
*.jpg binary
*.webp binary
*.keystore binary
*.apk binary
